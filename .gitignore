activations/
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
*.npz
!figures/**/*.npz

# scaled dataset (fetched structures + derived annotations, regenerable)
data/scaled/

# scaled embed intermediates (regenerable from gitignored activations)
figures/scaled/embed/

# diverse multi-organism dataset (regenerable)
data/diverse/
figures/diverse/embed/

# the paper (manuscript, figures, and figure-assembly) lives outside this
# reproducibility repo (in Overleaf); keep the whole directory out
paper/

# local Overleaf export bundle
/overleaf/
/overleaf.zip

data/experimental/

# figshare deposit staging (built locally, uploaded to figshare)
/figshare/
